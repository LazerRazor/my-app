#include <bits/stdc++.h>
using namespace std;
#define ll long long

void testcase()
{
    // God please don't let there be any errors
      
    int n;
    cin>>n;

    for (int i = 0; i < n; ++i)
    {
        cout<<i+1<<endl;
    }
}

int32_t main() 
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

    int tc = 1;
    // cin >> tc;
    while (tc--) testcase();
    return 0;
}